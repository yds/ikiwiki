* tag 0.2
  - put back loadplugin of filecheck, without which everything fails
  weirdly.

  - document dependency on patched filecheck

* tag 0.1

  - kinda sorta working.
  - HTML attachement handling could be improved. Most text/html 
  are deleted by default. Bad html is recognized as text/plain.

